import sitemapGet from "../../api/sitemap.get"

export default {
    async get(){
        //console.log(route, nuxtApp.$router.options.routes)
        //nuxtApp.$router.options.routes.forEach(item=>{
        //   // console.log(item)
        //})
        return 'fffff'
    }
}